<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!-- 注意：所有权限都已经迁移到：core：resources 模块AndroidManifest.xml -->

<!--     use to start http service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        android:minSdkVersion="34"
    />

<!--    use to start http service on system startup-->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- https://developer.android.com/about/versions/11/privacy/package-visibility -->
    <!-- for select app to enter pull/leave push -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"
    />


        <!-- android:icon="@mipmap/ic_launcher" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
        <!-- android:supportsRtl="true" -->
        <!-- tools:targetApi="26" -->
        <!-- android:requestLegacyExternalStorage="true" -->
                <!-- android:allowBackup="false" -->
                
    <application>
    
        <service
            android:name="com.catpuppyapp.puppygit.service.AutomationService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
        <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name="com.catpuppyapp.puppygit.service.HttpService"
            android:foregroundServiceType="specialUse"
            android:enabled="true"
            android:exported="true"
        >
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Http Service to do pull/push via http request"
            />
        </service>

        <service
            android:name="com.catpuppyapp.puppygit.service.TileHttpService"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@drawable/dog_head"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.catpuppyapp.puppygit.receiver.BootReceiver"
            android:exported="true"
            android:label="BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


        <!--        <meta-data-->
<!--            android:name="com.google.android.gms.ads.APPLICATION_ID"-->
<!--            android:value="ca-app-pub-7616157923976738~6641339390"-->
<!--        />-->
<!--        <meta-data-->
<!--            android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION"-->
<!--            android:value="true"-->
<!--        />-->
<!--        <meta-data-->
<!--            android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING"-->
<!--            android:value="true"-->
<!--        />-->
        <activity
            android:name="com.catpuppyapp.puppygit.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidIDE"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"

        >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:label="@string/import_str">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>

            </intent-filter>
            <intent-filter android:label="@string/import_str">
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>

            </intent-filter>

            <intent-filter android:label="@string/edit">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- support "file://" -->
                <data android:scheme="file" />
                <!-- support "content://" -->
                <data android:scheme="content" />

                <!-- mime type, can have more than 1 -->
                <data android:mimeType="text/*" />
                <data android:mimeType="*/*" />

            </intent-filter>

        </activity>

        <activity
            android:name="com.catpuppyapp.puppygit.activity.CrashActivity"
            android:exported="false"
            android:label="Crashed"
            android:theme="@style/Theme.AndroidIDE"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize">
        </activity>

<!--            android:name="androidx.core.content.FileProvider"-->
        <provider
            android:name="com.catpuppyapp.puppygit.provider.MyFileProvider"
            android:authorities="${applicationId}.puppygit.unique.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        
    </application>
</manifest>

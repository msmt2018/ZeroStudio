<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
     xmlns:tools="http://schemas.android.com/tools"
     android:installLocation="internalOnly"
     tools:ignore="Instantiatable">

     <!-- PR-D7: 调试器触觉反馈 (DebuggerHaptics) -->
     <uses-permission android:name="android.permission.VIBRATE" />
     <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

     <!-- 音频后台播放服务 (AudioPlaybackService, Media3 MediaSessionService) -->
     <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
     <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
     <uses-permission android:name="android.permission.WAKE_LOCK" />

     <permission
          android:name="com.itsaky.androidide.permission.BIND_LOG_SERVICE"
          android:protectionLevel="normal" />

     <application
          android:allowBackup="false"
          android:allowNativeHeapPointerTagging="false"
          android:enableOnBackInvokedCallback="true"
          android:hardwareAccelerated="true"
          android:icon="@mipmap/ic_launcher"
          android:roundIcon="@mipmap/ic_launcher_round"
          android:label="@string/app_name"
          android:largeHeap="true"
          android:memtagMode="sync"
          android:name=".app.IDEApplication"
          android:preserveLegacyExternalStorage="true"
          android:requestLegacyExternalStorage="true"
          android:resizeableActivity="true"
          android:supportsRtl="true"
          android:configChanges="screenSize|screenLayout|orientation|smallestScreenSize|density"
          android:theme="@style/Theme.AndroidIDE"
          android:networkSecurityConfig="@xml/network_security_config"
          android:dataExtractionRules="@xml/data_extraction_rules"
          android:fullBackupContent="@xml/backup_rules"
          android:appCategory="productivity"
          android:usesCleartextTraffic="true"
          tools:replace="android:allowBackup,android:label,android:theme,android:icon"
          tools:ignore="DiscouragedApi,MediaCapabilities,UnusedAttribute">
          <activity
               android:exported="true"
               android:name=".activities.CrashHandlerActivity"
               android:windowSoftInputMode="stateAlwaysHidden">
               <intent-filter>
                    <action android:name="com.itsaky.androidide.REPORT_CRASH" />
                    <category android:name="android.intent.category.DEFAULT" />
               </intent-filter>
          </activity>

          <activity
               android:exported="true"
               android:theme="@style/AppTheme.Splash"
               android:name=".activities.SplashActivity">
               <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
               </intent-filter>
          </activity>

          <activity
               android:exported="false"
               android:screenOrientation="portrait"
               android:configChanges="orientation|screenSize|screenLayout"
               android:name=".activities.OnboardingActivity"/>
          
          <activity
               android:exported="false"
               android:name=".activities.MainActivity"
               android:theme="@style/Theme.AndroidIDE" />

          <activity
               android:configChanges="orientation|screenSize"
               android:launchMode="singleTop"
               android:windowSoftInputMode="adjustResize|stateHidden"
               android:name=".activities.editor.EditorActivityKt" />

          <activity android:name=".activities.PreferencesActivity" />
          <activity android:name=".activities.AboutActivity" />
          <activity android:name=".activities.LottieAnimationSelectorActivity" />
          <activity android:name=".activities.ContributorsActivity" />
          <activity
               android:name=".activities.TerminalActivity"
               android:windowSoftInputMode="adjustResize" />

          <provider
               android:authorities="${applicationId}.documents"
               android:exported="true"
               android:grantUriPermissions="true"
               android:name=".provider.IDEDocumentsProvider"
               android:permission="android.permission.MANAGE_DOCUMENTS">
               <intent-filter>
                    <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
               </intent-filter>
          </provider>

          <provider
               android:authorities="${applicationId}.providers.fileprovider"
               android:exported="false"
               android:grantUriPermissions="true"
               android:name=".provider.IDEFileProvider">
               <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/ide_file_provider_paths" />
          </provider>

          <!-- ShizukuProvider: 接收 Shizuku server 下发的 binder。
               没有此 provider 注册, Shizuku.pingBinder() 永远返回 false,
               设备连接管理 BottomSheet 的 Shizuku 通道永远显示「未运行」。
               android:permission 必须是 Shell (com.android.shell) 持有但普通 app 没有的权限,
               android:exported 必须为 true (Shizuku server 在 adb/root 下运行需跨进程访问),
               android:multiprocess 必须为 false (Shizuku server 在 app 启动时拿 uid)。 -->
          <provider
               android:name="rikka.shizuku.ShizukuProvider"
               android:authorities="${applicationId}.shizuku"
               android:exported="true"
               android:multiprocess="false"
               android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

          <receiver
               android:enabled="true"
               android:exported="true"
               android:name=".services.InstallationResultReceiver"
               tools:ignore="ExportedReceiver">
               <intent-filter>
                    <action android:name="com.itsaky.androidide.installer.INSTALL_PACKAGE" />
               </intent-filter>
          </receiver>

          <service
               android:exported="false"
               android:foregroundServiceType="dataSync"
               android:name=".services.builder.GradleBuildService" />

          <service
               android:exported="true"
               android:name=".services.log.LogReceiverService"
               android:permission="com.itsaky.androidide.permission.BIND_LOG_SERVICE">
               <intent-filter>
                    <action android:name="com.itsaky.androidide.LOG_SERVICE_ACTION" />
               </intent-filter>
          </service>
          <service
               android:enabled="false"
               android:exported="false"
               android:name="androidx.appcompat.app.AppLocalesMetadataHolderService">
               <meta-data
                    android:name="autoStoreLocales"
                    android:value="true" />
          </service>

          <activity
               android:name=".activities.SdkManagerActivity"
               android:exported="false" />

          <service
               android:name=".services.sdk.SdkManagerService"
               android:foregroundServiceType="dataSync"
               android:exported="false" />

          <!-- 音频后台播放服务 (Media3 MediaSessionService) -->
          <!-- 接收系统 MediaButton / 接管锁屏通知 / 自动管理前台通知 -->
          <service
               android:name=".fragments.editor.audio.AudioPlaybackService"
               android:exported="false"
               android:foregroundServiceType="mediaPlayback">
               <intent-filter>
                    <action android:name="androidx.media3.session.MediaSessionService" />
                    <action android:name="com.itsaky.androidide.audio.START_PLAYBACK" />
               </intent-filter>
          </service>

          <profileable
               android:shell="true"
               tools:targetApi="q" />
          <meta-data
               android:name="firebase_performance_logcat_enabled"
               android:value="true" />
          <!-- lsp language Server  ## start -->
          <!-- <service -->
          <!-- android:name="android.zero.studio.lsp.servers.workspace.WorkspaceSymbolService" -->
          <!-- android:enabled="true" -->
          <!-- android:exported="false" -->
          <!-- android:process=":lsp_workspace_symbol"> -->
          <!-- <intent-filter> -->
          <!-- <action -->
          <!-- android:name="com.itsaky.androidide.lsp.SERVICE_ACTION_android-ide-workspace-symbol-lsp" /> -->
          <!-- </intent-filter> -->
          <!-- </service> -->

     </application>
</manifest>

# PR-8: Mock end-to-end tests for EvalEngine + SourceLocator

## 概要

延续 PR-7 的单元测试,这一轮把测试覆盖延伸到 **JDWP 层**:不再
只验证 `EvalEngine.Parser` 的 AST 形状,而是驱动整个
`EvalEngine.evaluate()` 路径,配上一个能预制响应的假
`JdwpClient`,覆盖 LOCAL / FIELD / METHOD / THIS / 字面量 /
CreateString / 错误路径。顺便把 `SourceLocator.fetchLocal()` 和
`Debugger.isTruthy()` 的覆盖也补齐。

## 改动 (7 files, +520 / -13)

```
ide-debugger/.../jdwp/JdwpClient.java                    |   5 +-
ide-debugger/.../api/Debugger.java                       |  27 +-
ide-debugger/.../jdwp/FakeJdwpClient.java (test)         | 117 +++ (new)
ide-debugger/.../jdwp/JdwpPayloads.java   (test)         | 222 +++ (new)
ide-debugger/.../model/EvalEngineEvaluateTest.java (test)| 308 +++ (new)
ide-debugger/.../model/SourceLocatorFetchLocalTest.java  | 211 +++ (new)
ide-debugger/.../api/DebuggerIsTruthyTest.java (test)    | 121 +++ (new)
```

## 关键设计

### 1. Mock 策略

- 去掉 `JdwpClient` 的 `final` 修饰符,允许测试在 `src/test`
  继承得到 `FakeJdwpClient`,覆盖 `sendCommand(cs, cmd, data)`
  即可。
- `Debugger` 仍为 `final`,但新增 **包私有构造器**
  `Debugger(JdwpClient client)` 接受外部客户端(默认无参构造器
  仍然 `this(new JdwpClient())`);生产代码无变化。
- `FakeJdwpClient` 按队列返回响应,并记录所有发送出去的命令,
  供测试断言顺序与数量。

### 2. JDWP payload 编码器

- `JdwpPayloads` 把 9 种常用响应(Frames / VariableTable /
  GetValues / ThisObject / Signature / ReferenceType / Fields /
  Methods / ClassesBySignature / CreateString)封装成可读辅助
  方法,免去手写 ByteBuf 样板。

### 3. `isTruthy` 提到包私有

- `Debugger.isTruthy(EvalResult)` 之前是 `private static`,
  现在改为 package-private,可以直接用各类型分支的 EvalResult
  做单元测试。

## 测试覆盖

`EvalEngineEvaluateTest` 19 个 case:
- LOCAL 原语:int / long / boolean
- LOCAL 对象:SourceLocator 把 object id 抹平为 0 的已知行为
- THIS 解析
- FIELD 解析(this.name)
- METHOD 解析(this.toString())
- 字符串字面量 + CreateString
- 整型 / 双精度字面量(零 JDWP 调用)
- 错误路径:local 不存在 / empty / trailing input /
  field 不存在 / class 没加载 / method 不存在 /
  方法带参数

`SourceLocatorFetchLocalTest` 9 个 case:
- 找到匹配的局部变量
- 找不到名字时回退到 `this`(走 ThisObject/ReferenceType/Signature)
- varCount == 0 直接返回 null
- 多种错误码路径(Frames / VariableTable / GetValues 失败)
- frameId 不匹配返回 null
- boolean / long 类型变体

`DebuggerIsTruthyTest` 19 个 case:
- BOOLEAN / INT / LONG / SHORT / BYTE / CHAR / FLOAT / DOUBLE
  的零值 vs 非零值
- OBJECT / ARRAY / STRING 的 null objectId vs 非零
- displayValue == null(错误结果)默认 truthy
- 解析失败的 displayValue 默认 truthy

## 验证

- ✅ `git diff --stat`: 7 files changed, 520 insertions, 13 deletions
- ✅ 所有测试文件的 import 已检查,无未使用符号
- ⏳ 沙箱无 Android SDK + Gradle wrapper 下载失败,完整 build
  在 PR review 时跑 `./gradlew :ide-debugger:test`

## 依赖

- **基于**: `pr-7-eval-unit-tests`
- **新依赖**: 无
- **包私有 API 变化**:
  - `JdwpClient`: `public final class` -> `public class`
  - `Debugger` 新增包私有构造器 `Debugger(JdwpClient)`
  - `Debugger.isTruthy(EvalResult)` 从 `private static`
    改为 package-private static

# PR-9: Expression evaluation with method arguments

## 概要

延续 PR-5 ~ PR-8 的表达式求值基础,这一轮解锁了**带参数的方法调用**。
之前 `invokeNoArgMethod` 拒绝所有带参调用,现在把参数求值后编
码进 `ObjectReference.InvokeMethod` 的请求,打通完整的
`expr.method(arg1, arg2, ...)` 路径。

## 改动 (3 files, +300 / -50)

```
ide-debugger/.../model/EvalEngine.java          | +250 / -30
ide-debugger/.../jdwp/JdwpPayloads.java         |  +15
ide-debugger/.../model/EvalEngineEvaluateTest.java | +90 (5 new cases)
ide-debugger/.../model/EvalEngineHelpersTest.java |  +170 (new)
```

## 关键改动

### 1. `invokeNoArgMethod` → `invokeMethod`

把方法调用从 0 参推广到任意参数:

```java
// Before (PR-8):
private EvalResult invokeNoArgMethod(long objectId, String methodName, String ownerSig) {
    // hard-coded argCount = 0
    buf.writeInt(0);
    return EvalResult.of(evalTag(ownerSig), ownerSig, v); // wrong return type!
}

// After (PR-9):
private EvalResult invokeMethod(long objectId, String methodName, String ownerSig,
                                List<EvalResult> argResults) {
    // 1. evaluate each arg recursively (short-circuits on error)
    // 2. lookup method: try exact sig first, then name+arity fallback
    // 3. write args with encodeValue() into the InvokeMethod packet
    // 4. use the response tag as return type (not the receiver sig!)
}
```

### 2. 新增辅助函数

| 函数 | 职责 |
|---|---|
| `buildSignature(argResults)` | 把参数列表拼成 `(IJLjava/lang/String;)V` 格式,作为 lookup hint |
| `encodeValue(buf, arg)` | 把 `EvalResult` 按 JDWP wire format 写入 ByteBuf |
| `tagToSignature(tag)` | JDWP tag byte → JVMS 签名字符串 |
| `countArity(msig)` | 统计方法签名参数个数,用于 name+arity fallback lookup |
| `lookupMethodByNameAndArity(refType, name, arity)` | 新增;现有 `lookupMethodByName` 忽略 arity |

### 3. 参数求值短路 (short-circuit)

```java
for (Resolved arg : r.args) {
    EvalResult a = resolveAndEval(arg, threadId, frameId);
    if (a.isError()) return a;   // 任何一个参数失败,整个调用失败
    argResults.add(a);
}
```

### 4. 返回类型从响应 tag 推导

之前错误地用 `ownerSig` (receiver 的类型) 作为返回值类型,
现在从 `InvokeMethod` 响应的 tag 推导出真实返回值签名:

```java
byte tag = in.readByte();
String returnSig = tagToSignature(tag);   // 用响应的 tag
return EvalResult.of(evalTag(returnSig), returnSig, v);
```

## 已知局限

- 所有数值字面量在解析器层存储为 `LITERAL_LONG`,所以
  `buildSignature` 对 `"(II)"` 和 `"(JJ)"` 都返回 `"(JJ)"`。
  解决: fallback `lookupMethodByNameAndArity` 按参数个数查找,
  匹配到第一个同名方法。
- String 参数的 `typeSignature` 是 `"Ljava/lang/String;"`,
  但 `tagToSignature('L')` 返回 `"Ljava/lang/Object;"`。
  对有参方法调用而言这是安全的(VM 只需要 objectId),
  但返回值如果是对象,其精确类型会丢失(有待后续 PR 跟进)。

## 测试覆盖

- `EvalEngineEvaluateTest` 5 个新 case:
  - `this.add(1, 2)` → 2 个原始 int 参数,验证 InvokeMethod 请求字节
  - `this.greet("")` → String 参数(先走 CreateString)
  - `this.increment(5)` → 单参,fallback lookup 路径
  - `this.missing(1)` → 方法不存在,返回错误
  - `this.add(x)` → 参数求值失败,短路返回错误
- `EvalEngineHelpersTest` 16 个纯函数 case:
  - `buildSignature`: 空/单/双/混合参数
  - `encodeValue`: boolean/int/long/float/double/object/emptySig/byte
  - `tagToSignature`: 所有 primitive tags + object tags + unknown
  - `countArity`: `(II)I`/`(Ljava/lang/String;J)V`/数组/混合/畸形签名

## 验证

- ✅ `git diff --stat`: 3 files changed, ~+300 / ~-50
- ⏳ 沙箱无 Android SDK,完整 build 在 PR review 时跑:
  `./gradlew :ide-debugger:test`

## 依赖

- **基于**: `pr-8-eval-mock-tests`
- **新依赖**: 无
- **内部 API 变化**:
  - `invokeNoArgMethod` → `invokeMethod` (private,无外部调用)
  - `buildSignature / encodeValue / tagToSignature / countArity`
    提升为 package-private static
  - `lookupMethodByNameAndArity` 新增 (private)
